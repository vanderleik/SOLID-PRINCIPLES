/**
 * ContaPoupancaLSP
 * Classe que representa uma conta poupança
 * Essa classe extende a ContaBancariaLSP e implementa o método sacar, que é abstrato na classe pai.
 */

public class ContaPoupancaLSP extends ContaBancariaLSP {

    @Override
    public void sacar(double valor) {
        // Lógica para saque de conta poupança, 
        // possivelmente com restrições específicas
    }
    
}
